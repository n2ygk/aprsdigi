Summary: AX.25 Automatic Position Reporting System aprsdigi and aprsmon
Name: aprsdigi
Version: @VERSION@
Release: 5
License: GPLv2+
Group: Utilities/System
Packager: Alan Crosswell <n2ygk@weca.org>
Buildroot: /var/tmp/aprsdigi
Source: http://sourceforge.net/projects/aprsdigi/files/aprsdigi/%{version}/aprsdigi-%{version}.tar.gz
Url: http://aprsdigi.sourceforge.net/
Requires: libax25 >= 0.0.7, systemd
BuildRequires: systemd-units

%changelog
* Sat Sep 08 2012 Alan Crosswell <alan@columbia.edu>
- Update to reflect hosting at sourceforge.net
- Track changes to rpm SPEC specs!
- Add systemd-based aprsdigi.service & aprsbeacon.service and remove aprsdigi.init
- Add logrotate

* Fri Aug 29 2009 Alan Crosswell <alan@columbia.edu>
- Update to use configure to set version, install initscript.

* Fri Dec 28 2001 Alan Crosswell <alan@columbia.edu>
- Try to make a new RPM, but first need to find RPMs for libax25.

* Mon Apr 05 1999 Alan Crosswell <alan@columbia.edu>
- Initial rpm

%description
Aprsdigi is a specialized Amateur Packet Radio (AX.25) UI-frame digipeater
for the Automatic Position Reporting System, APRS(tm).  

Aprsmon collects and displays standard AX.25 UI text frames in a format similar
to that output by a standard TNC in "Monitor ON" mode and is intended
to be used with programs like javAPRS which wish to see a TNC data
stream over a TCP connection.

%prep
%setup -q -n aprsdigi-%{version}

%build
./configure
make

%install
rm -rf $RPM_BUILD_ROOT
make DESTDIR=$RPM_BUILD_ROOT install 
#install -D -m 755 aprsdigi.init ${RPM_BUILD_ROOT}/etc/rc.d/init.d/aprsdigi
install -D -m 644 aprsdigi.service ${RPM_BUILD_ROOT}/usr/lib/systemd/system/aprsdigi.service
install -D -m 644 aprsbeacon.service ${RPM_BUILD_ROOT}/usr/lib/systemd/system/aprsbeacon.service
install -D -m 644 aprsdigi.logrotate ${RPM_BUILD_ROOT}/etc/logrotate.d/aprsdigi

%clean
rm -rf $RPM_BUILD_ROOT

%post
if [ $1 -eq 1 ] ; then 
 /usr/bin/systemctl enable aprsdigi.service >/dev/null 2>&1 || :
 /usr/bin/systemctl enable aprsbeacon.service >/dev/null 2>&1 || :
fi

%preun
if [ $1 -eq 0 ]; then
 /usr/bin/systemctl -q disable aprsdigi.service >/dev/null 2>&1 || :
 /usr/bin/systemctl -q stop aprsdigi.service >/dev/null 2>&1 || :
 /usr/bin/systemctl -q disable aprsbeacon.service >/dev/null 2>&1 || :
 /usr/bin/systemctl -q stop aprsbeacon.service >/dev/null 2>&1 || :
fi

%postun
if [ $1 -ge 1 ] ; then
 /bin/systemctl daemon-reload >/dev/null 2>&1 || :
 /usr/bin/systemctl -q try-restart aprsdigi.service >/dev/null 2>&1 || :
 /usr/bin/systemctl -q try-estart aprsbeacon.service >/dev/null 2>&1 || :
fi

%files
%doc README
%doc INSTALL
%doc COPYING
%doc NEWS
%doc ChangeLog
%doc TODO
%doc AUTHORS
%doc aprsdigi.lsm
%doc examples
/usr/sbin/aprsdigi
/usr/lib/systemd/system/aprsbeacon.service
/usr/lib/systemd/system/aprsdigi.service
/etc/logrotate.d/aprsdigi
/usr/sbin/aprsmon
/usr/share/man/man8
